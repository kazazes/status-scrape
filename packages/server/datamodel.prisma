type User {
  id: ID! @unique
  name: String!
  email: String! @unique
  password: String!
}

type StatusScrapeTarget {
  name: String!
  twitterHandle: String
  strategy: ScrapeStrategy! @default(value: "STATUSPAGE")
  statusUrl: String!
}
